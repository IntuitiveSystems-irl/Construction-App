diff --git a/node_modules/path-to-regexp/dist/index.js b/node_modules/path-to-regexp/dist/index.js
index 4c7f4b5..a1c8f91 100644
--- a/node_modules/path-to-regexp/dist/index.js
+++ b/node_modules/path-to-regexp/dist/index.js
@@ -1,3 +1,15 @@
+// PATCH: Add error handling to path-to-regexp
+const originalPathToRegexp = require('path-to-regexp');
+const pathToRegexp = function() {
+  try {
+    return originalPathToRegexp.apply(this, arguments);
+  } catch (error) {
+    console.error('\n🚨 PATH-TO-REGEXP ERROR 🚨');
+    console.error('Error parsing path:', arguments[0]);
+    console.error('Error details:', error.message);
+    throw error;
+  }
+};
 
     var isarray = Array.isArray || function (arr) {
   return Object.prototype.toString.call(arr) == '[object Array]';
@@ -70,6 +82,7 @@ var parse_1 = parse;
         }
         // Found the end of a segment with a parameter
         if (i === str.length || str[i] === '/' || str[i] === '.' || str[i] === ')' || str[i] === '?' || str[i] === '+' || str[i] === '*') {
+          console.log('\n🔍 PATH-TO-REGEXP DEBUG: Parsing parameter at position', i, 'in path:', str);
           if (last < i) {
             out += str.slice(last, i);
             last = i;
@@ -76,6 +89,7 @@ var parse_1 = parse;
           out += param ? "/([^/]+?)" : "/([^/]+?)";
           last = i + 1;
           if (i < str.length) {
+            console.log('🔍 PATH-TO-REGEXP DEBUG: Adding parameter at position', i, 'with value:', str.slice(i, i + 1));
             out += str[i];
             ++i;
           }
